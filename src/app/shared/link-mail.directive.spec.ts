import { LinkMailDirective } from './link-mail.directive';

describe('LinkMailDirective', () => {
  it('should create an instance', () => {
    //const directive = new LinkMailDirective();
    //expect(directive).toBeTruthy();
  });
});
